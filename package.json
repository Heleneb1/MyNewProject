{
  "name": "alexandre-dumas",
  "version": "1.0.0",
  "description": "alexandre dumas's project, the famous author",
  "config": {
    "cli": "npm"
  },
  "scripts": {
    "postinstall": "cross-env-shell \"husky install && cd ./frontend && $npm_package_config_cli install && cd ../backend && $npm_package_config_cli install\"",
    "migrate": "cd ./backend && node migrate.js",
    "dev": "npm run dev-front & npm run dev-back",
    "dev-front": "cd frontend && npm run dev",
    "dev-back": "cd backend && npm run dev",
    "lint": "echo '----- Checking Frontend...' && npx prettier --write . && eslint --ext .jsx . --fix && echo '----- Done!'",
    "fix": "cross-env-shell \"cd ./frontend && $npm_package_config_cli run fix && cd ../backend && $npm_package_config_cli run fix\""
  },
  "keywords": [],
  "author": "Helene",
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "husky": "^8.0.3",
    "lint-staged": "^16.2.7"
  },
  "dependencies": {
    "@react-three/drei": "^9.66.1",
    "@react-three/fiber": "^8.13.0",
    "argon2": "^0.30.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "joi": "^17.9.1",
    "js-cookie": "^3.0.5",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1",
    "react-icons": "^5.0.1",
    "react-three-fiber": "^6.0.13",
    "three": "^0.152.0"
  }
}
